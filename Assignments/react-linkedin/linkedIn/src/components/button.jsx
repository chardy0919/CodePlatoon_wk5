import React from 'react'


const Button = ({ name, style, onClick }) => {
  return (
    <button className="nav-button" style={style} onClick={onClick}>
      {name}
    </button>
    
  );
};

export default Button;















































































// ur gay

































